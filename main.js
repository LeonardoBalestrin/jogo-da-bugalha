//import Mesa from './ObjectMesa'
//const Mesa = require('./ObjectMesa');

function rollD6(){
    return Math.round(Math.random()*5)+1;
}

function catchPosition(){

}

addEventListener('click',function(event){
    let idObject = event.target.id;
})